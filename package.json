{
  "name": "stripe-gradient",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint:prettier": "prettier --write '**/*.{glsl,html,json,md,ts,yaml,yml,prettierrc}'",
    "lint:tsc": "tsc --project ./tsconfig.json",
    "lint:yamllint": "yamllint --strict ."
  },
  "devDependencies": {
    "@types/three": "^0.176.0",
    "prettier": "^3.5.3",
    "prettier-plugin-glsl": "^0.2.1",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "three": "^0.177.0"
  }
}
